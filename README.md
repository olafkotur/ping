# Ping

`python ping.py`

## Basic Usage
Pings a given host a fixed number of times (10) every 1000 milliseconds.
```
ping google.co.uk
```


## Custom Usage
Pings a given host a given number of times every 1000 milliseconds.
```
ping google.co.uk -c 50
```